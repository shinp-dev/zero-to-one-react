# 🏗️ システムアーキテクチャ ＆ 教育工学的設計

本プロジェクト『React Learn & Play』は、システム本体の開発言語として **TypeScript (TS)** を採用している一方で、学生が実際にコードを書いて学習するサンドボックス環境（プレイグラウンド）の教材コードには **JavaScript (JS) / JSX** を採用しています。

本ドキュメントでは、この「TypeScript と JavaScript をあえて混在させている（ハイブリッド設計）」アーキテクチャの背景にある、**技術的な理由**および**教育工学（プログラミング教育）上の狙い**を詳しく解説します。

---

## 🗺️ アーキテクチャ概要

本アプリは、フロントエンド完結型の「Web IDE（プログラム開発・実行環境）」です。

```
┌────────────────────────────────────────────────────────────────────────┐
│                        【ブラウザ（フロントエンド）】                   │
│                                                                        │
│ ┌───────────────────────────┐         ┌──────────────────────────────┐ │
│ │  ■ システム本体（親）     │         │  ■ サンドボックス（子）      │ │
│ │  ・React / TypeScript     │         │  ・学生が入力した JS / JSX   │ │
│ │  ・Monaco Editor (TS型)   │         │  ・ブラウザ上での動的実行    │ │
│ └─────────────┬─────────────┘         └──────────────▲───────────────┘ │
│               │                                      │                 │
│               │ (コード文字列の取得)                  │ (マウント・実行) │
│               ▼                                      │                 │
│ ┌────────────────────────────────────────────────────┴───────────────┐ │
│ │  ■ コンパイル・検証エンジン                                          │ │
│ │  1. @babel/standalone により JS/JSX を動的コンパイル (TS製ラッパー)   │ │
│ │  2. 仮想コンソールで console.log をインターセプト                    │ │
│ │  3. 拡張された検証用 DOM を用いたテスト実行 (TS製アサーション)       │ │
│ └────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 1. 教育工学的アプローチ：認知負荷の最小化 (Pedagogical Reason)

モダンWeb開発において TypeScript は業界標準（デファクトスタンダード）ですが、**「プログラミングを学び始める学生」に最初からTSを強制することは、教育効果を著しく低下させるリスク**があります。

### 🚨 「React」と「TS」の二重の壁
初心者が React と TypeScript を同時に学習すると、以下のような「認知負荷の過負荷（ダブル・ラーニング・カーブ）」が発生します。
- **文法の混同**: 「これはReactの作法（JSXやHooksのルール）なのか、それともTypeScriptの文法（型定義）なのか」の境界が曖昧になります。
- **コンパイルエラーによる挫折**: `useState` の初期値に `null` を入れただけで、複雑な型エラー（例：`Type 'null' is not assignable to type 'string'...`）が発生し、Reactの面白さに気づく前にエラーとの不毛な戦いで疲弊してしまいます。

### 💡 解決策としての「JS/JSX サンドボックス」
学生が入力するエディタ環境を **JavaScript（JSX含む）** に制限した理由はここにあります。
- 学生は「状態（State）が変わると画面が再描画される」「関数（コンポーネント）間でPropsがバケツリレーされる」という、**React本来の仕組みと概念（エッセンス）の理解だけに100%集中**できます。
- 文法をシンプルに保つことで、「書いて、ボタンを押して、すぐ動く」という学習のテンポ（クイックフィードバック）を最大化します。

---

## 2. 技術的アプローチ：インブラウザ実行の最適化 (Technical Reason)

ブラウザ上だけで動かす「フロントエンド完結型」のアーキテクチャを実現する上でも、このハイブリッド設計は非常に合理的です。

### ① インブラウザ・コンパイルの軽量化
学生が書いたプログラムをリアルタイムにコンパイルして実行するために、本アプリは **`@babel/standalone`** を使用しています。
- **Babel（JSコンパイラ）**: JSX や ES6+ などの標準外構文を、ブラウザが直接動かせるピュアな JavaScript へ一瞬でトランスパイル（翻訳）します。非常に軽量で、スマートフォンや低スペックな学生用PCでも遅延なく（0.1秒以下で）動作します。
- **TypeScriptコンパイラ (tsc) をブラウザで動かす場合のデメリット**:
  ブラウザ上で本格的な TypeScript の型チェック（Type Checking）を行おうとすると、重厚な TS コンパイラエンジン全体をブラウザにロードする必要があり、初期読み込みに数秒〜数十秒の遅延（数MB以上のJSファイルのダウンロード）が発生し、UXが著しく低下します。

### ② 安全かつシンプルな動的テスト実行
学生が書いたコードは、内部的に `new Function()` や `eval` に近い仕組みでブラウザ上のメモリ空間で実行されます。
JavaScript であれば、型情報を全て削ぎ落としたピュアな実行ファイルとしてネイティブに動作するため、検証システム（`src/data/lessons.ts` 内の `validate` 関数）による DOM アサーションやログのキャプチャが非常にシンプルかつ高速に行えます。

---

## 3. なぜ「システム本体」は TypeScript なのか？ (System Architecture)

学生向けサンドボックスは JavaScript ですが、それを取り囲む **Web IDE システム全体（エディタの制御、進捗バー、コンソール、Babelのラッパー、レイアウト管理）はすべて TypeScript (Vite + React + TS) で開発**されています。

その理由は、**システムの堅牢性と拡張性を担保するため**です。

1. **Monaco Editor（エディタ）との親和性**:
   VS Codeと同エンジンの Monaco Editor は、TypeScript で開発されています。エディタの各種オプションや挙動をバグなく実装するためには、TSによる型定義が必須でした。
2. **レッスンデータの構造安全化**:
   全19章の教材データ（`lessons.ts`）は、タイトル、タスク、初期コード、模範解答、そして高度なテスト検証ロジック（`validate`）を保持しています。このデータが1箇所でも欠けたり型がズレたりするとアプリ全体がクラッシュするため、`export interface Lesson` を定義し、TypeScriptコンパイラによってビルド時に完璧に安全性を検証しています。
3. **長期的な機能追加のしやすさ**:
   将来的に教材を追加したり、UIレイアウトを組み替えたり、新しい検証ロジックを追加する際に、静的解析がバグを100%防ぎます。

---

## 🌟 未来のロードマップ（TSへの架け橋）

学生に「この教材は、なぜ最初から TypeScript ではないのか？」と聞かれた際の、指導者向けのアプローチ方法です。

1. **まず本システム（1章〜19章）で React の「型のない自由度と、その危うさ」を学ぶ**
   - 「スペルミスをしてもエラーにならず画面が崩れてしまう」「Propsに違うデータを入れても気づきにくい」という体験をあえてさせます。
2. **ローカル環境で TypeScript を導入する（第2ステップ）**
   - 本教材を修了した後に、Vite等で `React + TS` のプロジェクトを新規作成させます。
   - 「さあ、みんなはもう React の仕組み（StateやProps、Context）をマスターしたね。ここからは、プロの現場と同じように **TypeScript の盾** を装備して、バグを未然に防ぐプロの書き方を学ぼう！」
   - この順序（**Reactの機能理解 ➔ TSによる安全化**）で導入すると、学生はTSの「型を書く面倒くささ」よりも「守られているありがたみ（自動補完やエラー防止）」を強く実感し、スムーズに実務レベルのエンジニアへステップアップできます。
